<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link rel="icon" href="imgs/IconColour.png">
	<title>Register - Arrif</title>

	<link rel="stylesheet" href="style/base.css">
	<link rel="stylesheet" href="style/forms.css">
</head>

<body>
	<%- include("templates/navbar") %>

	<section class="credentials-form responsive-flex">
		<div class="credentials-form-container responsive-container">
			<h1>Sign up</h1>
			<form class="login-form" action="register" method="post">
				<%- locals?.formError?.e ? `<a class="form-error">${formError.e}</a>`: ""; %>
				<input type="text" aria-label="login input" name="username" placeholder="username" required>
				<input type="text" aria-label="email input" name="email" placeholder="email" required>
				<input type="password" aria-label="password input" name="password" placeholder="password" required>
				<input type="submit" value="submit">
				<%- locals?.formError?.n ? `<script>document.querySelector(".login-form input[name='${formError.n}']").style.borderColor = "red";</script>`:""; %>
			</form>
		</div>
	</section>

	<%- include("templates/footer") %>
</body>

</html>